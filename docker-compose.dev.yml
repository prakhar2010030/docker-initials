version: "3"
services:
  node-app:
    build:
      context: .
      args:
        NODE_ENV: development
    volumes:
      - ./:/app
      - /app/node_modules
    environment:
      - NODE_ENV=environment
      - MONGO_USER=root
      - MONGO_PASSWORD=password
    command: npm run dev
